{{- if .Values.adminApi.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: admin-api
spec:
  selector:
    app: axum-admin-api
  ports:
    - name: http
      port: 9000
      targetPort: http
{{- end }}
