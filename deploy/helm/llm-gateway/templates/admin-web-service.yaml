{{- if .Values.adminWeb.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: admin-web
spec:
  selector:
    app: admin-web
  ports:
    - name: http
      port: 80
      targetPort: http
{{- end }}
